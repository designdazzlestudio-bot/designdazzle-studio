# Required: create a Gemini API key in Google AI Studio.
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE

# Optional. Current project default:
GEMINI_MODEL=gemini-3.5-flash-lite

# Local development only (Vercel manages its own port/host):
PORT=3000
HOST=0.0.0.0
